
```bash
# ls -l | gr -v dr

activate                # $PATH에 ./utils/ 등록
install.sh              # gcc 7, qemu 설치 스크립트
LICENSE
Make.config
Makefile
Makefile.build
Makefile.kernel
Makefile.userprog
README.md
```


- `./activate`
    - set up your environment with below command.
    - better append on `.bashrc` like `source ./activate`
